apiVersion: v1
kind: Secret
metadata:
  name: tms-postgres-secret
type: Opaque
stringData:
  POSTGRES_DB: temperature_monitoring
  POSTGRES_USER: tms_user
  POSTGRES_PASSWORD: tms_password
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: tms-postgres-secret
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
